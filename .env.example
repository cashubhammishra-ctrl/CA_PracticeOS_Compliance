# Anthropic Claude API
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Tally MCP connection (ICAI Tally Prime MCP Server) - see connectors/connector_setup.md
# Local-demo only: leave both blank to use the cached fallback data instead
# (the Tally MCP Server is a local Node.js process, unreachable from a cloud deployment).
TALLY_MCP_COMMAND=
TALLY_MCP_ARGS=
