Live app (submit this as the primary link): https://cashubhammishra-ctrl.github.io/CA_PracticeOS_Compliance/CA_PracticeOS_Compliance.html
Live backend API: https://ca-practiceos-backend.onrender.com
Live Streamlit demo URL (secondary/legacy, not the primary submission surface): https://ca-practiceos-streamlit.onrender.com
Video link (unlisted YouTube/Drive): [pending - record and paste before ZIP submission]

Verified live 2026-09-20: full auth flow tested end-to-end against the public
GitHub Pages URL, the deployed Render backend, and a permanent Supabase
Postgres database (not SQLite, not Render's free 30-day database) - signup,
admin-approval email via Resend, approve link, login, JWT session, tenant
data isolation between two separate test firms, and role-based team member
permissions (non-admin blocked from adding members, 403 confirmed).

Responsive design verified on 2026-09-20 across desktop (1280px+), tablet
(768px), and mobile (375px) viewports - off-canvas hamburger nav, full-width
modals on mobile, horizontally-scrollable data tables, no layout breakage.

PWA install support confirmed: manifest.json + service worker registered,
installable via the browser's own "Install app" (desktop) / "Add to Home
Screen" (mobile) prompts.

The AI Agent Layer (Agent 1 Onboarding, Agent 2 Drafting, Agent 3
Compliance - the headline feature) and the Tally Connector are built
directly into this one app (CA_PracticeOS_Compliance.html), not a separate
tool - per the explicit decision to submit L1 as the single capstone
artifact. The AI Agents call the same deployed FastAPI backend listed above.
